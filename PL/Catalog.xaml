<Window x:Class="PL.Catalog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Catalog" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="GroupHeaderStyle" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupItem}">
                        <Expander x:Name="expander" IsExpanded="True"
                                  Background="Transparent"
                                  Foreground="#2b331f">
                            <Expander.Header>
                                <TextBlock Text="{Binding Name}"/>
                            </Expander.Header>
                            <ItemsPresenter/>
                        </Expander>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--// Text font style //-->

        <Style x:Key="font_style" TargetType="TextBlock">

            <Setter Property="FontFamily"
                    Value="Fonts/Roboto-Light.ttf #Roboto"/>

            <Setter Property="VerticalAlignment"
                    Value="Center"/>

            <Setter Property="HorizontalAlignment"
                    Value="Left"/>

            <Setter Property="Foreground"
                    Value="Black"/>

            <Setter Property="FontSize"
                    Value="20"/>

        </Style>
        <!--// Tooltip Style Template //-->

        <Style TargetType="ToolTip" x:Key="tt_style">

            <Setter Property="Placement" Value="Right"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="20,0,0,0"/>
            <Setter Property="Background" Value="Lavender"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontFamily" Value="Fonts/Roboto-Light.ttf #Roboto"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="PlacementRectangle" Value="60,0,0,0"/>
            <Setter Property="HorizontalOffset" Value="15"/>
            <Setter Property="VerticalOffset" Value="6"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>

        </Style>

        <!--// App Close Button Style Template //-->

        <ImageBrush x:Key="CloseButton_MouseOver"/>

        <Style x:Key="Close_btn" TargetType="{x:Type Button}">

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="{x:Type Button}">

                        <Border x:Name="button"
                                BorderThickness="0"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True">

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter Property="Background"
                                        TargetName="button"
                                        Value="{StaticResource CloseButton_MouseOver}"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>
        <Style x:Key="tb_style" TargetType="{x:Type ToggleButton}">

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="{x:Type ToggleButton}">

                        <Border  x:Name="border"
                                 BorderThickness="{TemplateBinding BorderThickness}"
                                 Background="{TemplateBinding Background}"
                                 SnapsToDevicePixels="True">

                        </Border>

                        <ControlTemplate.Triggers>



                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>



    </Window.Resources>

    <Grid x:Name="MainScreenCatalog">
        
        <Grid x:Name="ProductCatalog">

        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
           <ColumnDefinition/>
           <ColumnDefinition Width="50*"/>
           <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
            <ComboBox x:Name ="CategorySelecter" SelectionChanged="CategorySelecter_SelectionChanged" IsEditable="True" IsReadOnly="True" Text="Select a category" SelectedItem="" Margin="5,5,20,275" Grid.Row="1"/>

            <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,45,0,0" Grid.Column="1" Grid.Row="1">
            <Image Source="bigchallah.jpeg"></Image>
        </Button>
        <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,145,0,0" Grid.Column="1" Grid.Row="1">
            <Image Source="rolls.jpg" RenderTransformOrigin="0.5,0.5" Height="73" Width="87">
            </Image>
        </Button>
        <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,45,0,0" Grid.Column="2" Grid.Row="1">
            <Image Source="cccookies.jpeg"></Image>
        </Button>
        <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,45,0,0" Grid.Column="3" Grid.Row="1">
            <Image Source="potatokugel.jpeg"></Image>
        </Button>
        <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,145,0,0" Grid.Column="3" Grid.Row="1">
            <Image Source="potatoes.jpeg"></Image>
        </Button>
        <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,245,0,0" Grid.Column="3" Grid.Row="1">
            <Image Source="greenbeans.jpeg"></Image>
        </Button>
        <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,145,0,0" Grid.Column="2" Grid.Row="1">
            <Image Source="sugarcookies.jpeg"></Image>
        </Button>
        <Button Width="150" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,245,0,0" Grid.Column="2" Grid.Row="1">
            <Image Source="brownies.jpeg"></Image>
        </Button>
            
        <Button x:Name="ShoppingCart" Grid.Column="1" Content="View Shopping Cart" HorizontalAlignment="Left" Margin="50,7,0,0" Grid.Row="1" VerticalAlignment="Top" Click="ShoppingCart_Click"/>
        <!--<Button Grid.Column="1" Content="Button" HorizontalAlignment="Left" Margin="20,22,0,0" Grid.Row="1" VerticalAlignment="Top" Height="55" Width="145"/> -->



            </Grid>

        </Grid>


</Window>


